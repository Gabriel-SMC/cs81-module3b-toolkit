Using the examples of 5, -3, and 0, the function (double(n)) will result with a positive answer, a negative answer, and a zero respectively.

Using the examples of 8, -14, and 0, the function (square(n)) will result with two positive answers and a zero respectively.

Using the examples of 19.7, -94, and 0, the function (isEven(n)) will result with all answers being true.

Using the examples of 12.6, -76.52, and 0, the function (isOdd(n)) will result with two being true and one being false.

Usig the examples of 7, -16.4, and 0, the function (multiply(n)) will result with two positive answers respectively and a zero.

Using the examples of 14, -9, and 0, the function (addition(n)) will result with a positive answer, a negative answer, and positive answer respectively.